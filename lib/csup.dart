import 'package:flutter/material.dart';

// implements a drawing screen with a center reset button.

class CSup extends StatefulWidget {
  const CSup({super.key});
  @override
  State<StatefulWidget> createState() => CSupState();
}

class CSupState extends State<CSup> {
  @override
  Widget build(BuildContext context) {
      return
        const Scaffold(
        );
  }
}

